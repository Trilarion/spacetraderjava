package org.winforms;

import java.awt.*;

// TODO what is this good for
public interface SystemColors { // TODO this is never written and should be a constants class (private constructor) instead of an interface
    Color Control = null;
    Color ControlText = null;
    Color Info = null;
}
